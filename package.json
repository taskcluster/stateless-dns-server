{
  "name": "stateless-dns-server",
  "version": "0.2.1",
  "scripts": {
    "start": "node server.js",
    "test": "mocha --ui tdd --reporter spec test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/taskcluster/stateless-dns-server.git"
  },
  "dependencies": {
    "buffer-read": "^0.0.2",
    "buffer-write": "^0.0.2",
    "lodash": "^4.17.19",
    "native-dns": "^0.7.0",
    "thirty-two": "^0.0.2"
  },
  "devDependencies": {
    "mocha": "^6.2.0"
  },
  "engines": {
    "node": ">=8.15.x"
  }
}
